Переменные:

    //Простыми словами переменная это
    //ячейка памяти с именем куда можно
    //положить число, букву, текст
    
    //Как делать переменные:
    
    //Я уже писал про типы, но еще
    //я не писал про сами переменные.
    
    //Пример переменной:
    
    //---------------------
    
        int a;
        int b;
        
        //Мы видим 2 переменные a и b
        //они оба с типом int
        
        //Можно писать не как на примере,
        // а написать int a, b; но
        //это работает если у переменных
        //один тип данных.

        /*
         Сначала мы пишем тип переменной
         в этом случае тип переменной это int,
         дальше мы пишем название переменной
         например a, b или как вам захочется,
         вот мы написали например int a;
         мы объявили переменную, но не написали
         что в ней находится, чтоб присвоить
         переменной значение мы просто дописываем
         например = 1, например int a = 1,
         мы присвоили значение переменной.
         
         Вот шаблон:
         
         тип название = значение
         
         Пример:
         
         float num = 3.2342f;
         
        */
    //---------------------
    
    //Правила:
    
    // - Название переменной не должно
    //начинаться на цифру или число,
    //переменная всегда начинается на
    //букву.
    
    // - Нельзя писать '$' в названии
    //переменной.
    
    // - Нельзя называть переменную
    //на зарезервированное слово 
    //такое как class.
    
    // ПРАВИЛЬНО:
    //int age = 25;
    //float price = 9.99;
    //char name[20] = "Alex";

    // НЕПРАВИЛЬНО:
    //int 1age = 25;    // нельзя начинать с цифры
    //float price$ = 9.99; // нельзя использовать $
    //char class = 'A';  // class — зарезервированное слово
    
    //Информацию о типах вы можете найти в той же папке
    //там где и лежит этот файл.
    
    
